
fun main (args : Array<String>){

    val names = listOf<String>("Addam", "Arman", "Firdaus");

    println("Names " + names[0])
    println("Index of 1: " + names.get(1))
    println("Is List empty?: " + names.isEmpty())
}